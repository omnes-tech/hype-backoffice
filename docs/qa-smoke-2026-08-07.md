# Smoke test — correções do tasks.md (2026-08-07)

Checklist de validação manual em **staging** de tudo que foi corrigido. Marque
cada item ao passar. Legenda de repo: **API** (hypeapp-api), **BO**
(hypeapp-backoffice), **APP** (hypeapp-app).

## Pré-requisitos (fazer ANTES)
- [ ] Deploy da **API** + **BO** (republicar) + build do **APP** em staging.
- [ ] Rodar migrations: **`086_get_campaign_management_is_external.sql`** e
      **`087_backoffice_influencer_profile_contact.sql`**.
- [ ] Envs: `EMAIL_ENABLED=true` + SMTP (`EMAIL_HOST/USER/PASSWORD/FROM`) para #6.
- [ ] Envs LiveKit: `LIVEKIT_URL/API_KEY/API_SECRET` + Egress
      (`LIVEKIT_EGRESS_S3_*`, `LIVEKIT_EGRESS_PUBLIC_BASE_URL`) para #39/#40/#41;
      BO servido em **https** (senão `getUserMedia` da câmera não roda).

---

## Lote A — Campanhas

- [ ] **#63/#56/#57 Envio de conteúdo / HEIC** (APP): num iPhone, envie um
      conteúdo com foto **HEIC** (galeria) e também um vídeo/PDF. Esperado:
      upload conclui (sem "não corresponde ao formato"). Cole um link do Drive
      (pasta) e do YouTube Shorts → aceitos.
- [ ] **#54 Botão de envio** (APP): na fase de conteúdo, o botão **"Enviar
      conteúdo"** aparece proeminente (roxo, ícone upload) e funciona.
- [ ] **#50 Roteiro obrigatório por fase** (API): numa campanha multifase, ao
      entrar na 2ª fase, tentar enviar conteúdo **sem** roteiro aprovado →
      bloqueado ("envie o roteiro desta fase..."). Após aprovar o roteiro da
      fase, o envio libera.
- [ ] **#51 Fase atual** (API/APP): a tela de progresso mostra a fase correta
      (baseada em `currentStepId`, não pela data).
- [ ] **#49 Aprovar/reprovar conteúdo** (BO): aprovar e reprovar um conteúdo →
      persiste ao recarregar (não volta ao estado anterior).
- [ ] **#64 Data de entrega da fase** (APP): a data limite mostrada ao
      influenciador **bate** com a configurada na campanha (sem 1 dia a menos).
- [ ] **#62 Card some no kanban** (BO): um influenciador em qualquer status
      (inclusive envio/permuta) sempre aparece numa coluna; nenhum some.
- [ ] **#58 Aba "Minhas campanhas"** (APP): abre em **"Ativas"** (não em
      "Inscrições" vazia).
- [ ] **#59 Deep-link reprovação de roteiro** (APP): tocar na notificação de
      reprovação de roteiro abre a **página da campanha** (não uma tela genérica).
- [ ] **#53 Endereço da permuta no perfil** (BO): "Ver perfil" de um
      influenciador com permuta mostra a seção de endereço/contato.

## Lote B — Chat / Notificações / Mural

- [ ] **#17 Links clicáveis** (APP): mensagens com `https://`, `http://`,
      `www.` e domínio nu (`instagram.com/x`) viram link clicável; segurar copia.
- [ ] **#10 Caixa do chat** (APP): a caixa de digitação é maior (multi-linha).
- [ ] **#15 Enter no chat** (APP/BO): Enter quebra linha; envio é pelo botão
      (BO: Ctrl/⌘+Enter).
- [ ] **#19 Data da mensagem** (APP/BO): mensagens mostram data + hora.
- [ ] **#16 Link do grupo pelo chat** (BO): no chat do influenciador, botão de
      grupos insere o `share_url` do grupo na mensagem.
- [ ] **#3 Notificação de chat** (BO): clicar na notificação de chat abre o
      chat/kanban **direto** (sem precisar clicar de novo).
- [ ] **#11/#21 Mensagem some do sino após ler** (BO+APP): abrir o chat marca as
      notificações de mensagem daquela conversa como lidas (some do sino).
- [ ] **#4 Notificações por workspace** (BO): com 2 workspaces, o sino mostra só
      as do workspace atual (notificações de conteúdo enviado aparecem).
- [ ] **#12 Ícone de chat no header** (BO): o ícone morto foi **removido**.
- [ ] **#20 Sinal de não-lida na campanha** (APP): a lista "Minhas campanhas"
      tem badge; e a aba **Chat** dentro da campanha mostra um ponto quando há
      não-lida (some ao abrir o chat).

## Lote C — Kanban

- [ ] **#29 Roteiro reprovado** (BO): reprovar roteiro (individual **e em lote**)
      → o influenciador vai para a coluna **"Roteiro em Correção"** (sai de
      "Aguardando Aprovação Roteiro").
- [ ] **#30 Remover de Aprovados** (BO): no card em "Aprovado", botão **"Remover
      da campanha"** abre o modal de recusa e retira a pessoa.
- [ ] **#31 Etiqueta Externo** (BO): influenciador externo mostra a etiqueta
      **"Externo"** no card do kanban (requer migration 086).

## Lote D — Lives

- [ ] **#37 Apagar live** (BO): apagar uma live `upcoming/ended/cancelled`.
- [ ] **#38 Curtir** (APP): coração fica **vermelho** ao curtir; descurtir baixa
      o contador; não dá pra "curtir infinito".
- [ ] **#43 Compartilhar** (APP): botão de compartilhar abre o share nativo com
      o link da live.
- [ ] **#42 Chat da live no BO** (BO): na sala de controle, o chat conecta e
      recebe/mostra mensagens (requer deploy do gateway).
- [ ] **#44 Curtida na sala de controle** (BO): curtidas do app aparecem no
      contador da sala em tempo real.
- [ ] **#41 RTMP (OBS→live)** (BO): com a live **ativa**, botão "Transmitir via
      OBS (RTMP)" retorna Servidor + Stream Key; configurar no OBS e transmitir
      → aparece na live (requer LiveKit Ingress + Egress).
- [ ] **#39/#40 Câmera/áudio** (INFRA): com Egress configurado + BO em https,
      o estúdio publica e o app assiste (HLS). *É config, não código.*

## Lote E — Grupos

- [ ] **#25 Capa do grupo** (BO+APP): no grupo da campanha, "Adicionar/Alterar
      capa" envia imagem; o app mostra a capa (não o placeholder cinza).
- [ ] **#26 Botões do grupo** (APP): "Criar post" e "Sair" ficam proporcionais.
- [ ] **#23 Massa** (BO): selecionar vários e "Adicionar selecionados" ao grupo.
- [ ] **#24 Auto-add ao aprovar** (BO): aprovar um influenciador com grupo
      `auto_join_on_approval` → entra no grupo automaticamente.

## Lote F/G — Cadastro / E-mail

- [ ] **#2 Busca por @** (BO): buscar influenciador digitando `@handle` encontra.
- [ ] **#1/#34 Contato no perfil** (BO): "Ver perfil" mostra **e-mail e
      telefone** (seção Contato), inclusive para externos (requer migration 087).
- [ ] **#5 Rascunhos** (BO): filtro **"Rascunhos"** lista as campanhas draft; a
      aba "Ativas" não mistura rascunhos.
- [ ] **#6 E-mail de convite de workspace** (API): convidar um membro → e-mail
      chega (requer SMTP configurado).
- [ ] **#35 Baixar app** (BO): no pré-cadastro do convite, em iPhone o botão
      abre a **App Store** (não só Play Store).
- [ ] **#7 Jornada de e-mails** (API): inscrição via link dispara e-mail de
      confirmação; aprovado/recusado dispara os respectivos; D7/D20/D30 para
      quem não baixou.

## #33 — Usuários duplicados (AUTO-MERGE) — testar com atenção

- [ ] Crie um usuário **externo** por link de premiação com um `@handle` (ex.:
      Instagram `@maria`), participando de 1 campanha.
- [ ] Cadastre uma conta **nova no app** (e-mail diferente) e **conecte** a
      mesma rede `@maria`.
- [ ] Abra a lista de redes no app (dispara o auto-merge). Esperado:
  - [ ] A participação/roteiros/conteúdos/endereço do externo migraram para a
        conta do app.
  - [ ] A conta externa ficou com `deleted_at` (sumiu).
  - [ ] **Nada ficou órfão** (conferir no banco).
- [ ] Caso de **sobreposição** (ambos na mesma campanha): o merge **NÃO**
      acontece (aborta) — validar que nada foi movido.
- [ ] Fallback manual (super admin): `POST /backoffice/admin/users/:dupId/merge-into/:survivorId`.

---

## Automatizado (rodar em CI/staging)
- **Unit** (`npm test` em cada repo): API `lib/utils/*.spec.ts` (url, gate,
  phase, civil-date, upload-options, external-duplicate-match, user-merge-plan);
  BO `management-kanban-config.test.ts`; APP `notification-routing.test.ts`,
  `linkify.test.ts`, `campaigns-initial-tab.test.ts`.
- **E2E de DB** (API, precisa `DATABASE_URL` de teste): `npm run test:e2e`
  (inclui `user-merge-db.e2e-spec.ts` do #33).
